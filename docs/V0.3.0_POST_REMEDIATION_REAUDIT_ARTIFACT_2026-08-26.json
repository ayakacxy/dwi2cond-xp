{
  "surface": "report",
  "manifest": {
    "version": 1,
    "surface": "report",
    "title": "dwi2cond-xp v0.3.0 整改后独立流程、算法与计算逻辑复审报告",
    "description": "dwi2cond-xp v0.3.0 对 SimNIBS 4.6.0 与 FSL 6.0.4 的独立算法复审",
    "generatedAt": "2026-08-26T00:00:00+08:00",
    "cards": [],
    "charts": [
      {
        "id": "severity_distribution",
        "title": "确认问题按严重度分布",
        "subtitle": "截至 2026-08-26，共 27 条；数量不等同风险，P0/P1 优先整改",
        "type": "bar",
        "dataset": "severity_counts",
        "sourceId": "issue_inventory",
        "encodings": {
          "x": {
            "field": "severity",
            "type": "ordinal",
            "label": "严重度"
          },
          "y": {
            "field": "issue_count",
            "type": "quantitative",
            "label": "问题数",
            "format": "number"
          }
        },
        "yAxisTitle": "问题数",
        "valueFormat": "number",
        "layout": "full"
      }
    ],
    "tables": [],
    "sources": [
      {
        "id": "issue_inventory",
        "label": "dwi2cond-xp v0.3.0 独立复审问题清单",
        "path": "V0.3.0_POST_REMEDIATION_REAUDIT_REPORT_2026-08-26.md",
        "query": {
          "engine": "duckdb",
          "language": "sql",
          "sql": "SELECT * FROM (VALUES ('P0', 2, '标准有效输入核心控制流'), ('P1', 10, '有效分支、正式结果与科学QA合同'), ('P2', 13, '边界、复现与低风险辅助产物'), ('P3', 2, '元数据与诊断')) AS issue_inventory(severity, issue_count, scope)",
          "description": "按复审报告中27个唯一问题ID汇总严重度。",
          "executed_at": "2026-08-26T00:00:00+08:00"
        }
      }
    ],
    "blocks": [
      {
        "id": "title",
        "type": "markdown",
        "body": "# dwi2cond-xp v0.3.0 整改后独立流程、算法与计算逻辑复审报告\n\n> 审计日期：2026-08-26\n>\n> 审计对象：当前公开 `dwi2cond-xp v0.3.0`，解引用 commit\n> `678e6a26f8fb202130f7d0c2f5f36ee19fe2cd0d`\n>\n> 官方基线：SimNIBS 4.6.0 `dwi2cond` 与 FSL\n> `6.0.4:ddd0a010`\n>\n> 审计性质：对 `D2C-030-001..014` 整改后的重新独立审计；本轮只出具问题报告，\n> 不修改算法实现\n>\n> 总体结论：**当前 v0.3.0 的默认主流程骨架、有限正常输入下的 WLS、张量分量合同、\n> 自动 T1 affine 的张量重定向和 `dir/vn/mc` 电导率计算已与官方实现形成较强对应；但仍\n> 不能认定为“与官方相同逻辑”或“端到端数值等价”。本轮确认 27 条新问题记录（含若干\n> 问题簇）：其中两个 P0 EDDY 控制流问题可改变标准有效输入的运动、异常值替换、旋转后 bvec 和\n> corrected DWI；其余问题分布在 strict DTIFIT、shell/TOPUP、公开配准 API、数据谱系、\n> mask/metadata、pipeline cache 和 FEM 状态合同。**\n\n本报告审计的是同名替换发布后的 commit `678e6a2`，不是原始发布快照 `7d47a3f`。\n[原始快照独立审计](V0.3.0_INDEPENDENT_ALGORITHM_AUDIT_REPORT_2026-08-26.md)\n及其 [整改报告](V0.3.0_AUDIT_REMEDIATION_REPORT_2026-08-26.md) 仍可作为历史证据，\n但其中“14 项均关闭”不能外推为当前 tag 已不存在其他算法或合同问题。"
      },
      {
        "id": "section_1",
        "type": "markdown",
        "body": "## 1. 执行摘要\n\n### 1.1 对用户三个核心问题的直接回答\n\n| 审计问题 | 复审判定 |\n| --- | --- |\n| 算法流程是否与官方相同 | **主干相近，但 EDDY 不是同一控制流。** legacy、nomoco、最终 WLS、FLIRT/FNIRT→vecreg→T1 mask 的阶段顺序基本对应；EDDY 缺失 interspersed-b0 运动初始化，且最终 `--repol` 检查位于错误位置。 |\n| 具体算法实现是否一致 | **部分一致。** 有限正常输入 WLS、给定正确 FSL matrix 的 affine vecreg、PPD 核心公式和 `dir/vn/mc` 有强正证据；EDDY shell/final repol、TOPUP iout、public world-transform basis 和 strict-FSL 部分 NaN 均确认不一致。 |\n| 计算逻辑是否一致 | **不能认定一致。** 部分 NaN 的真实 FSL A/B 已产生 tensor relative L2 `0.27845`、S0 relative L2 `234.66`；当前公开 EDDY endpoint 的 corrected-DWI relative L2 为 `0.00819`，且 parameters/replacement 控制流已有源码反例。 |\n| 是否可以继续宣称“v0.3.0 与官方实现相同” | **不可以。** 可表述为“固定子集的多数阶段已复现，若干局部算子通过 A/B”；官方兼容性 P0/P1 关闭并补齐同输入阶段级门禁前，不应表述为完整同流程或完整数值等价；可靠端到端产品声明还需另行关闭 cache/FEM 状态合同。 |\n\n### 1.2 最优先阻断等价结论的五个问题\n\n1. **D2C-030-016（P0）：EDDY 缺失 interspersed-b0 到 DWI 的运动初值插值。**\n   官方在 b0 registration 后、DWI registration 前按 acquisition volume 位置插值 b0\n   movement；当前实现把所有 DWI movement 初始化为零。\n2. **D2C-030-017（P0）：EDDY 最终 `--repol` 的位置和 GP fudge factor 均不同。**\n   官方在 PEAS shell alignment 后以 fudge factor `1` 做 final outlier check；当前实现在\n   PEAS 前以默认 `10` 检测并替换。\n3. **D2C-030-015（P1）：strict-FSL 部分 NaN 的 mean/S0 clamp 语义不同。**\n   当前实现使用有限值均值，FSL `Sum()/Nrows()` 传播 NaN；真实 `dtifit` A/B 已证明会\n   改变 tensor、S0 和 SSE。\n4. **D2C-030-018（P1）：single-shell 判断既不等于 FSL grouping，项目内部也不一致。**\n   同一组 b-values 可在上游 validator、EDDY 和 FSL 得到三个不同结论。\n5. **D2C-030-029（P1）：公开 world-transform API 可在不同 NIfTI basis 下旋转错 tensor。**\n   有效 oblique fixture 的真实 vecreg A/B relative L2 为 `0.37796`；自动 T1 主路径安全，\n   但公开 CLI/API 及后续方向性 conductivity 受影响。\n\n### 1.3 问题分布\n\n| 严重度 | 数量 | ID |\n| --- | ---: | --- |\n| P0 | 2 | `016,017` |\n| P1 | 10 | `015,018..022,029,031,038,039` |\n| P2 | 13 | `023..025,027,028,032..037,040,041` |\n| P3 | 2 | `026,030` |\n\n“27 条记录”包含默认主路径缺陷、公开 API/控制分支缺陷、异常输入合同和 metadata/QA\n问题；其中少数记录包含同一整改单元内的多个紧密子差异，因此不是 27 个严格原子根因，\n也不表示它们都会在普通全脑二值-mask 数据上同时触发。严重度反映对正式科学结果和\n“官方等价”声明的风险；不能用低优先问题数量稀释两个 P0，也不能因主路径正常就忽略\npublic API 的静默错误。"
      },
      {
        "id": "severity_chart_block",
        "type": "chart",
        "chartId": "severity_distribution",
        "layout": "full"
      },
      {
        "id": "section_2",
        "type": "markdown",
        "body": "## 2. 冻结基线、官方来源与审计方法\n\n### 2.1 当前 tag 身份\n\n审计开始时子仓库工作树干净，引用如下：\n\n```text\nHEAD        678e6a26f8fb202130f7d0c2f5f36ee19fe2cd0d\norigin/main 678e6a26f8fb202130f7d0c2f5f36ee19fe2cd0d\nv0.3.0^{}   678e6a26f8fb202130f7d0c2f5f36ee19fe2cd0d\ntag object  5d6355e0f8f5892b7c194ca91fd304ad5b7af561\n```\n\n同名 `v0.3.0` 曾先指向 `7d47a3fe59250d45c05104b5f9f27d097c39c6fe`，后被替换。\n因此任何审计、A/B、发布或回归结论都必须绑定 commit，而不能只写版本字符串。\n\n### 2.2 官方基线\n\n官方脚本来自本机未修改的 SimNIBS 4.6 环境；关键 SHA-256 为：\n\n| 文件 | SHA-256 |\n| --- | --- |\n| `dwi2cond` | `0978b26ab1c4d4d303b10151dbe4d0da8ed0c080c3d09af0855412c7b5c46340` |\n| `dwi2cond.prepro.source.sh` | `4671e6f72b0dd0da496297e722df3e3682e659dab9c06502c83907fb287f8f78` |\n| `dwi2cond.functions.source.sh` | `b133dd7e41ecac773d0af983bacb3f23b62fd2c91f84379ff81df42f89ee5932` |\n| `dwi2cond.t1reg.source.sh` | `539d1951621111495792c0957705494fc592579a9c0f3dc807dd6549a904205d` |\n| `dwi2cond.check.source.sh` | `29c3c907cd174bc628b3770feafb69831d4f4c76b41c91a97e1cfd938b655541` |\n\nFSL 根目录为 `/usr/local/fsl`，版本为 `6.0.4:ddd0a010`。流程对照 SimNIBS shell；\n具体计算和控制流同时对照 `/usr/local/fsl/src/fdt`、`/usr/local/fsl/src/eddy`、\n`vecreg.cc` 和 armawrap 实现。这样可避免只按命令名猜测语义。\n\n### 2.3 审计顺序\n\n本轮严格按以下顺序执行：\n\n1. 冻结当前 tag、官方脚本及 FSL 源码版本；\n2. 从 SimNIBS entrypoint 还原 raw/prefit、correction、DTIFIT、T1 registration、PPD、\n   post-mask 和 conductivity/FEM 边界；\n3. 逐阶段核对当前 workflow 的输入、输出、mask、bvals/bvecs、变换及调用顺序；\n4. 深入到 WLS、EDDY、PPD、conductivity、cache 和 QA 的具体公式与分支；\n5. 对可隔离差异运行真实 FSL A/B 或最小反例；\n6. 把“确认缺陷”“主动差异”“证据不足”和“已排除误判”分开记录。\n\n### 2.4 证据等级\n\n- **E3：真实同输入 A/B**。本地实现和 FSL/SimNIBS 实际运行并比较输出；\n- **E2：源码控制流或公式直接反例**。两侧源码足以证明不同；部分问题另有可执行探针；\n- **E1：本地合同/测试缺口**。能证明缓存、QA 或 API 自身不满足声明，但尚未形成完整\n  官方 endpoint A/B；\n- **未闭合**：只有局部测试或历史结果，不能据此判定完整等价或完整错误。\n\n除非另有说明，本报告的 `relative L2` 定义为\n`||current - FSL||₂ / ||FSL||₂`，在对应表格注明的 whole-array、mask 或 common-support\n上计算；`max abs` 使用同一 support。若参数列物理单位不同，则拆列报告或明确只把合并值\n作为原始参数表诊断，不能把它解释为单一物理误差。"
      },
      {
        "id": "section_3",
        "type": "markdown",
        "body": "## 3. 官方流程与当前实现的阶段映射\n\n### 3.1 SimNIBS 4.6 固定流程\n\n```text\nprefit tensor\n└─ copy/reorient → tensor_decomp → T1 registration\n\nraw single-shell DWI\n├─ canonical reorientation；复制 bvals/bvecs\n├─ exact b==0 → MCFLIRT 6 DOF → mean b0 → BET -f 0.2\n├─ correction 前 raw DTIFIT WLS（FA/SSE QA）\n├─ correction branch\n│  ├─ nomoco\n│  ├─ EDDY without TOPUP\n│  ├─ TOPUP → EDDY\n│  └─ legacy two-pass registration，optional GRE/FUGUE\n├─ corrected DWI `-thr 0`\n└─ DTIFIT --wls --sse --save_tensor\n\nT1/head model\n├─ labeling 1..499 → T1 brain mask\n├─ primary FLIRT 6/12 DOF\n├─ linear vecreg 或 FNIRT subsamp=8,4,2,2 → nonlinear vecreg/PPD\n├─ 无条件 T1 brain post-mask\n├─ tensor_decomp\n├─ corrected/raw 6 DOF FA/SSE QA\n└─ m2m_<subject>/DTI_coregT1_tensor.nii.gz\n\nSimNIBS conductivity/FEM consumer\n└─ tensor sampling → FSL basis correction → dir/vn/mc → SimNIBS FEM\n```\n\n关键官方位置：\n\n- raw canonical、exact-b0、BET、raw WLS：`dwi2cond.prepro.source.sh:33-100`；\n- no-TOPUP/TOPUP EDDY：`dwi2cond.prepro.source.sh:117-143`；\n- legacy/GRE、`-thr 0`、DTIFIT：`dwi2cond.prepro.source.sh:146-177`；\n- T1/FNIRT/vecreg/post-mask/QA：`dwi2cond.t1reg.source.sh:19-65`；\n- diffusion-only mean 的 exact `b>0`：`dwi2cond.functions.source.sh:327-351`。\n\n### 3.2 逐阶段判定\n\n| 官方阶段 | 当前 v0.3.0 | 判定 |\n| --- | --- | --- |\n| raw canonical reorientation | 各正式预处理 producer 已接入 | 主干一致 |\n| exact-b0 alignment + BET | nomoco、legacy、EDDY/TOPUP 均有对应 producer | 主干一致；raw QA 后续使用的 mask 谱系有误 |\n| correction 前 raw WLS | `fit_raw_dti_qa` | 阶段存在；GRE/EDDY 时错误复用 corrected mask |\n| nomoco | `run_nomoco_nifti` | 主干一致；聚合 QA 读取的 corrected artifact 不是 producer 产物 |\n| legacy without/with GRE | `run_legacy_nifti` | 旧审计的 warp/mask 问题已修复；公开 regression gate 通过，但不是全体素数值等价证据 |\n| TOPUP | 固定 `b02b0_nosubsamp.cnf` 子集 | schedule 和局部算子有证据；`iout` 公共强度恢复错误，完整 coefficient/movement→EDDY endpoint 未闭合 |\n| EDDY | 固定单壳子集 | **控制流不一致**：见 `016..018,031,040` |\n| post-correction `-thr 0` | `_write_nonnegative_nifti` | 一致 |\n| DTIFIT WLS | `fit_dti_nifti` | finite、二值 mask、finite grad-dev 输入高度一致；部分 NaN/mask/grad-dev/header 不一致 |\n| FLIRT/MCFLIRT 6/12 DOF | local optimizer | 普通 3-D 主干一致；薄层 `fix2D` 缺失，不能由 legacy regression gate 推导所有输入等价 |\n| FNIRT + PPD | fixed four-level FNIRT + PPD | 主工作流阶段顺序一致；公共 FNIRT API 可省略 mandatory post-mask |\n| tensor decomposition | `decompose_tensor6` | 分量/派生量主合同一致 |\n| affine vecreg | `register_tensor_affine` | 自动 T1 路径和给定 FSL matrix 时一致；公开 world matrix 基底、非有限值和 mask 边界有差异 |\n| `dir/vn/mc` | `tensors_to_conductivity` | finite 真实小网格同输入 A/B 达浮点舍入级一致 |\n| FEM | `run_tdcs` extension | 公式 consumer 对应；真实 solve 未闭环，输入/动态输出/cache/失败状态仍有缺陷 |"
      },
      {
        "id": "section_4",
        "type": "markdown",
        "body": "## 4. 确认问题：核心算法与控制流\n\n严重度定义：\n\n- **P0**：标准有效输入上可静默改变正式 DWI/tensor 或其关键变换，直接阻断等价结论；\n- **P1**：特定有效分支、正式数值/科学 QA 输出、输入/API 合同或缓存完整性可改变结果\n  或正式科学判读；\n- **P2**：复现身份、异常或尚未支持的输入边界、低风险辅助 artifact，或尚未形成正式\n  结果反例的完整性风险；\n- **P3**：不改变科学数组，仅错误记录元数据或诊断值。\n\n### D2C-030-015（P1，E3）：strict-FSL 部分 NaN 的 mean/S0 clamp 语义错误\n\n**当前实现**：[`tensor_fit.py:164-175`](../src/dwi2cond_xp/tensor_fit.py#L164) 对信号\n使用有限值求和除以 finite count；该均值参与初始和最终 S0 clamp。\n\n**官方实现**：`/usr/local/fsl/src/fdt/dtifit.cc:331-349` 使用\n`S.Sum()/S.Nrows()`。本机 armawrap 的\n`functions_base.hpp:66-70` 将 `Sum` 实现为 `arma::accu`；只要一项为 NaN，均值即为\nNaN，后续 `< mean` 比较为 false。当前 strict NIfTI 路径允许部分 NaN 进入拟合，见\n[`nifti_fit.py:131-142`](../src/dwi2cond_xp/nifti_fit.py#L131) 和\n[`nifti_fit.py:416-426`](../src/dwi2cond_xp/nifti_fit.py#L416)，因此不是不可达分支。\n\n**真实 FSL A/B fixture**：每个 voxel 的 11 个 volume 为：\n\n```text\n[1, 2, 100, 200, 300, NaN, 500, 600, 700, 800, 900]\n```\n\n其中前两个为 b0，后九个为 b=1000。结果：\n\n| 输出 | current v0.3.0 | FSL 6.0.4 | 差异 |\n| --- | ---: | ---: | ---: |\n| tensor | — | — | relative L2 `0.2784486688`；max abs `0.0016427911` |\n| S0 | `410.2999878` | `1.74110115` | relative L2 `234.6554579` |\n| SSE | `22.3955612` | `113.5842285` | relative L2 `0.80282860` |\n\n复现目录：`/tmp/d2c-nanmean-audit.jBzIJD/`。现有 NaN 单元/FSL 测试使用平滑物理信号，\n错误 clamp 分支没有触发，所以通过不能反驳该反例。\n\n**影响**：只影响包含部分 NaN 且触发 clamp 的 voxel；正常有限输入不受影响。但\n`strict-fsl` 名称承诺的是 FSL 语义，不能用更“鲁棒”的 finite mean 静默替代。\n\n**验收要求**：复现 FSL 的 NaN 传播和 `MaximumAbsoluteValue` 行为；增加部分 NaN、\nall-NaN、Inf、全非正信号以及 workers=1/2 的真实 tensor/S0/SSE A/B。更鲁棒行为只能\n保留在显式 `robust` 模式。\n\n### D2C-030-016（P0，E2）：EDDY 缺失 interspersed-b0 运动初始化\n\n**当前控制流**：[`eddy.py:3045-3088`](../src/dwi2cond_xp/preprocessing/eddy.py#L3045)\n先拟合 b0，随后调用 DWI 迭代，但没有把 b0 movement 插值结果传给 DWI；\n[`eddy.py:2623-2633`](../src/dwi2cond_xp/preprocessing/eddy.py#L2623) 因此把所有 DWI\nmovement 初始化为零。\n\n**FSL 控制流**：\n\n- `/usr/local/fsl/src/eddy/ECScanClasses.cpp:902-908`：当 b0 数量大于 2、首个 b0 位于\n  前 25%、末个 b0 位于后 25% 时认定为 interspersed；\n- 同文件 `:924-932`、`:1516-1539`：按 acquisition volume 位置 bracket 并线性插值\n  b0 movement，写入每个 DWI 的初值；\n- `/usr/local/fsl/src/eddy/eddy.cpp:534-536`：该步骤位于 b0 registration 后、DWI\n  registration 前。\n\n**为什么现有门禁未检出**：公开 synthetic EDDY fixture 在\n`tools/generate_synthetic_eddy_fixture.py:177-178` 只有两个开头 b0，官方分支永远不会\n触发。\n\n**影响**：对有效的 interspersed-b0 采集，非线性 DWI 优化从不同初值出发，可改变\nmovement/EC 轨迹、corrected DWI、`.eddy_parameters` 和 rotated bvecs。这里不要求证明\n每个数据集最终都不同；初始状态和官方控制流已确定不同。\n\n**验收要求**：构造至少 12 个 volume，在 `0、N/2、N-1` 放置有已知渐进刚体运动的\nb0；比较 DWI 第 0 次更新前 movement、完整 history、最终 parameters、rotated bvecs 和\ncorrected DWI。FSL 应显式固定 `--initrand=1`。\n\n### D2C-030-017（P0，E2）：EDDY final `--repol` 的顺序及 GP fudge factor 不一致\n\n**当前顺序**：\n\n1. [`eddy.py:2931`](../src/dwi2cond_xp/preprocessing/eddy.py#L2931) 应用 DWI location\n   reference；\n2. [`eddy.py:2934-2945`](../src/dwi2cond_xp/preprocessing/eddy.py#L2934) 立即执行 final\n   outlier detect/replace；\n3. 返回外层后，[`eddy.py:3092-3122`](../src/dwi2cond_xp/preprocessing/eddy.py#L3092)\n   才估计和应用 PEAS shell alignment。\n\nfinal check 的 `load_prediction_state()` 使用\n[`estimate_spherical_gp_hyperparameters`](../src/dwi2cond_xp/preprocessing/eddy.py#L319)\n默认 `error_variance_fudge_factor=10.0`。\n\n**FSL 顺序**：\n\n1. `/usr/local/fsl/src/eddy/eddy.cpp:102-132` 先 location reference，再做 PEAS；\n2. 同文件 `:320-327` 在 PEAS 后才运行 `FinalOLCheck`；\n3. final check 前把默认 fudge factor 从 `10` 临时改为 `1`，完成后恢复；\n4. `eddy.cpp:796-819` 用这组重新估计的 GP prediction 执行最终 detect/replace。\n\n**影响**：当前 outlier map 在错误的 movement 状态上计算，且预测误差方差是官方 final\ncheck 的十倍；反过来，当前 PEAS 又会看到已被提前替换的数据。可能同时改变：\n\n- PEAS translation/rigid 参数；\n- final outlier map 和 replacement values；\n- outlier-free scan-space data；\n- `.eddy_parameters`、rotated bvecs 和最终 corrected DWI。\n\n**现有测试为什么不足**：complete-runner 测试 monkeypatch 了 DWI registration 并设置\n`replace_outliers=False`；冻结 fixture 的 seeded hyperparameter 比较验证的是普通 factor\n10，不是 final factor 1；四个强人工坏片离阈值过远，即使顺序错误也可能同样被检出。\n\n**验收要求**：锁定调用序列\n\n```text\nDWI iterations → location reference → PEAS → final GP(ff=1)\n→ detect → replace → final resample\n```\n\n并增加接近 4σ 阈值、带非零 shell 刚体错位的 fixture；同输入比较 PEAS、final outlier\nmap、outlier-free data、parameters、rotated bvecs 和 corrected DWI。\n\n### D2C-030-018（P1，E2）：single-shell 判定不是 FSL shell-group 语义，且项目内部不一致\n\n当前 [`eddy.py:2977-2984`](../src/dwi2cond_xp/preprocessing/eddy.py#L2977) 要求任意两个\ndiffusion b-value 的差均 `<100`；更上游的\n[`gradients.py:57-77`](../src/dwi2cond_xp/gradients.py#L57) 又使用\n`max(shell)-min(shell) <= 100`。因此差值恰好 `100` 时，上游 validator 接受、EDDY 拒绝。\n\nFSL `/usr/local/fsl/src/eddy/EddyUtils.cpp:50-80` 则先按模板值把每个 b-value 分配到\n`<100` 的 group，再计算 group mean，并以 mean 做最终 assignment；它不是任意两值比较，\n也不是简单的全局 range。\n\n最小反例：\n\n```text\nb-values: [1000, 901, 1099]\ncurrent pairwise rule: false\nFSL grouping: one shell, group mean 1000, true\n```\n\n这里三值用于隔离 shell predicate；形成满足 EDDY 方向数要求的完整输入时，可把三值重复\n两次并加入 b0，判定差异不变。\n\n当前实现会拒绝 FSL 接受的固定单壳输入；在 exact-100 边界还会在两个本地入口得到不同\n结论。现有测试只覆盖两值恰好相差 `99.999/100`，没有覆盖 group-template 传递性或\n两个本地 validator 的一致性。\n\n**验收要求**：移植 FSL group/mean/assignment 语义，并加入上述三值反例、输入顺序置换、\n边界 `<100`/`==100`/`>100` 测试。"
      },
      {
        "id": "section_5",
        "type": "markdown",
        "body": "## 5. 确认问题：数据谱系、正式输出与 API\n\n### D2C-030-019（P1，E2）：raw QA 的 DWI/mask 网格和谱系混用\n\n官方在 distortion correction 前，用 canonical `DWIraw` 和 raw exact-b0 BET mask 运行\nraw `dtifit`：`dwi2cond.prepro.source.sh:35-37,67-100`。corrected mask 之后写入另一目录，\n不会覆盖 raw mask。\n\n当前有两个相互关联的问题：\n\n1. [`workflow.py:720-795`](../src/dwi2cond_xp/preprocessing/workflow.py#L720) 的 raw WLS\n   在预处理阶段之后运行，并复用统一变量 `dwi_mask`。legacy+GRE 中 raw mask 先在\n   [`legacy.py:421-431`](../src/dwi2cond_xp/preprocessing/legacy.py#L421) 产生，后在\n   [`legacy.py:572-577`](../src/dwi2cond_xp/preprocessing/legacy.py#L572) 被 corrected mask\n   覆盖；TOPUP+EDDY 同样把 TOPUP-derived corrected mask 赋给 `dwi_mask`。普通 no-TOPUP\n   EDDY 的内部自动 mask 仍来自 raw exact-b0，外部 mask 则属于显式扩展。因此不是所有\n   EDDY 分支都受影响，但 legacy+GRE、TOPUP+EDDY 和 external-mask 路径中的所谓 raw WLS\n   可能使用非 raw support。\n2. raw WLS 会先把 `config.data` canonical reorient，但聚合 QA 在\n   [`workflow.py:1056-1067`](../src/dwi2cond_xp/preprocessing/workflow.py#L1056) 又直接把\n   未 reorient 的 `config.data` 与 canonical/corrected `dwi_mask` 配对。\n   [`qa.py:312-340`](../src/dwi2cond_xp/preprocessing/qa.py#L312) 只按数组下标应用 mask，\n   没有检查 affine 或 grid。nomoco 更把 `corrected_dwi` 指回原始 `config.data`，而不是\n   producer 实际写出的 canonical `DWIforfit.nii`。\n\n**可执行几何反例**：同 shape `(4,4,2,3)`、非 canonical 轴置换下，mask voxel\n`[3,0,0]` 的 raw direct mean 为 `3.0`，canonical mean 为 `0.0`；shape 检查不会报警，\n但统计 silently 取错空间位置。复现文件位于 `/tmp/dwi2cond-qa-grid-probe/`。\n\n**影响**：不改变最终 corrected tensor，但会改变 raw FA/SSE support、raw/corrected mean\n统计和 6 DOF raw QA，破坏“校正前后比较”的科学含义。\n\n**验收要求**：显式分离 `raw_canonical_dwi/raw_bet_mask` 与\n`corrected_dwi/corrected_mask`；所有 QA 输入必须引用 producer artifact，并在 shape 之外\n核对 affine/grid。增加同 shape 轴置换、GRE 边界位移和 nomoco canonical fixture。\n\n### D2C-030-020（P1，E2）：公开 FNIRT API 允许省略官方 mandatory post-mask\n\n官方 `dwi2cond.t1reg.source.sh:37-49` 无条件执行：\n\n```text\nFNIRT → vecreg → T1 brain mask → tensor_decomp\n```\n\n当前 full pipeline 会传入 T1 mask，因此 CLI 主 DAG 安全；但公开导出的\n[`register_tensor_fnirt_nifti`](../src/dwi2cond_xp/preprocessing/nonlinear.py#L618) 把\n`brain_mask_file` 设为可选，随后原样传给\n[`register_tensor_nonlinear_nifti`](../src/dwi2cond_xp/preprocessing/nonlinear.py#L756)。\n后者只有 mask 非空时才清零 T1 脑外 tensor，见\n[`nonlinear.py:503-508`](../src/dwi2cond_xp/preprocessing/nonlinear.py#L503)。该函数又由\n[`preprocessing/__init__.py:179-186`](../src/dwi2cond_xp/preprocessing/__init__.py#L179)\n公开导出。\n\n**影响**：库调用者可在没有任何警告的情况下生成不满足官方最终 tensor 合同的文件。\n\n**验收要求**：官方完整 FNIRT wrapper 必须要求 brain mask；若要保留通用低层 PPD API，\n应以不同函数/显式模式暴露，并在名称和 manifest 中说明不是完整 SimNIBS branch。\n\n### D2C-030-021（P1，E2）：FEM stage 只声明 JSON，真实求解产物不受 cache 保护\n\n[`workflow.py:988-1028`](../src/dwi2cond_xp/preprocessing/workflow.py#L988) 的每个 FEM stage\n只声明 `dwi2cond_xp_simulation.json` 为 output。实际 mesh、subject-volume/HDF5 等由\n[`simulation.py:288-305`](../src/dwi2cond_xp/simulation.py#L288) 动态产生，路径只写进 JSON，\n没有内容哈希。\n\nPipeline cache 只验证 `StageDefinition.outputs`，见\n[`pipeline.py:271-295`](../src/dwi2cond_xp/preprocessing/pipeline.py#L271)；聚合 QA 也只把四个\nmanifest 作为静态输入，真实 FEM 文件是在 QA action 内动态读取。因此：\n\n- 删除某个真实 FEM output，`fem_*` stage 仍可能 cache-hit；\n- 篡改真实 output，manifest hash 和 stage fingerprint 不变；\n- 如果此前 QA manifest/图片仍存在，`pipeline_qa` 也可能 cache-hit，不会重新审计。\n\n**影响**：正式 FEM 结果与 cache/provenance 可分离，属于静默完整性错误；当前 fake FEM\n测试不能关闭该风险。\n\n**可执行状态机反例**：一个 FEM-like stage 只声明 JSON、action 另写动态文件；首轮\ncompleted 后删除动态文件，第二轮仍返回 `cached`，action 总执行次数保持 `1`，动态文件\n继续不存在。复现目录：`/tmp/dwi2cond-pipeline-audit.zRwGh2/`。\n\n**验收要求**：FEM manifest 必须记录每个动态 output 的解析路径、类型、shape 和 SHA-256；\ncache-hit 前展开并复核这些产物。增加“删除一个 volume”“同 shape 篡改 HDF5/NIfTI”后\n必须重算或显式失败的测试。"
      },
      {
        "id": "section_6",
        "type": "markdown",
        "body": "## 6. 确认问题：流水线、复现与 QA\n\n### D2C-030-022（P1，E2）：stage 失败后可遗留旧 output 并在下一次被当成新结果\n\n[`PipelineRunner.run_stage`](../src/dwi2cond_xp/preprocessing/pipeline.py#L297) 先直接执行\naction，再验证正式 output；失败时只原子写 failed manifest，不隔离或清除 action 已写出的\n正式路径。\n\n最小状态机反例：\n\n1. 第一次 action 写出 outputs A、B，然后抛异常；B 留在正式目录；\n2. 第二次 action 只写出 A 并正常返回；\n3. `validate_artifacts` 看到新 A + 旧 B，stage 被记录为 completed。\n\n实际探针得到 `A=A2`、`B=B-stale`、最终 `status=completed`，不是只根据代码推测；\n复现目录同为 `/tmp/dwi2cond-pipeline-audit.zRwGh2/`。\n\n**影响**：任何非原子多文件 stage 都可能发布跨 attempt 混合产物。输出哈希本身无法证明\n同一次 action 生成了全部文件。\n\n**验收要求**：优先在 attempt-specific 临时目录执行并整体原子发布；至少也要在运行前\n精确清理本 stage 声明的旧 outputs。增加“首轮部分写后失败、次轮漏写一个文件”的两次\n运行测试。\n\n### D2C-030-023（P2，E2）：聚合 QA 默认 b0 阈值与官方 exact-zero 合同不同\n\n官方 raw b0 只接受 `b==0`，diffusion mean 使用 `b>0`。当前\n[`build_pipeline_qa`](../src/dwi2cond_xp/preprocessing/qa.py#L264) 默认\n`b0_threshold=50.0`，即 `<=50` 算 b0、`>50` 算 diffusion。\n\n最小反例 `[0,20,1000]`：官方 diffusion mean 包含 `20`，当前 QA 把它放入 b0 mean。\n这不改变正式拟合，但使 QA artifact 不再复现官方定义。\n\n**验收要求**：官方兼容流水线显式传 `0.0`；如需常规 MRI 的阈值策略，应作为独立 QA\n模式并写入 manifest，而不是默认冒充官方定义。\n\n### D2C-030-024（P2，E1）：cache identity 未包含数值运行时和底层线性代数实现\n\n[`pipeline.py:245-266`](../src/dwi2cond_xp/preprocessing/pipeline.py#L245) 的 identity 由输入、\n依赖、参数、backend、包版本和 Python 源文件组成。Python/platform 仅在 stage 完成后写入\nruntime，见 `:356-366`，不进入 fingerprint；NumPy、SciPy、Numba、NiBabel、BLAS/LAPACK、\n线程后端和关键 FSL/SimNIBS 版本也没有完整进入 key。\n\n**影响**：同一 output 目录在更换数值环境后可能直接 cache-hit，不能证明结果属于当前\n运行时。对迭代优化器、BLAS eigensolver、Numba backend 和严格复现尤其不安全。\n\n**验收要求**：把会改变数值的依赖版本、Numba threading layer 和 BLAS/LAPACK identity\n纳入本地数值 stage 的 cache key；FSL/SimNIBS reference identity 只进入实际调用 reference\n的 A/B stage 与验证记录，不能让不调用它们的纯本地 stage 无条件失效。硬件只需记录，\n不必无条件使纯 CPU reference cache 失效。增加相关环境 identity 改变导致 cache miss 的\n单元测试。\n\n### D2C-030-025（P2，E1）：部分公开 config 被记录但不消费\n\n`Dwi2CondPipelineConfig` 对所有模式接受 `random_seed`，且\n[`workflow.py:661-672`](../src/dwi2cond_xp/preprocessing/workflow.py#L661) 把它写入 preprocess\nidentity；但只有 EDDY action 消费该值。legacy/nomoco/prefit 传入不同 seed 会改变 cache\nkey，却不会改变计算。反向情形中，非 prefit API 也能附带 `prefit_tensor` 而不被拒绝。\n\n**影响**：用户和 manifest 会误以为参数实际控制了算法，也产生无意义 cache miss；后者还\n允许一项未消费输入静默存在。\n\n**验收要求**：按 mode 拒绝未消费参数；或只在消费它的 mode 中暴露/记录。增加每个公开\nconfig 字段“被消费或显式拒绝”的参数化测试。\n\n### D2C-030-026（P3，E2）：SimNIBS conductivity QA 把总元素数记为四面体数\n\n[`simnibs_adapter.py:58-60`](../src/dwi2cond_xp/simnibs_adapter.py#L58) 调用真实 SimNIBS\n`mesh.elm.get_tetrahedra()`，其默认返回长度为 `mesh.elm.nr` 的布尔 mask；但\n[`simnibs_adapter.py:94-105`](../src/dwi2cond_xp/simnibs_adapter.py#L94) 使用\n`len(tetrahedra)`，因此写入总 element 数量，不是 mask 中 true 的数量。现有 fake 返回\n索引数组 `[0,2]`，模拟错了 API 并掩盖问题。\n\n**影响**：不改变 conductivity field，只错误记录 QA `tetrahedra`。\n\n**验收要求**：使用 `np.count_nonzero(tetrahedra)`；在同时含 triangle/tetra 的真实网格上\n验证计数，并确认 conductivity 数组不变。\n\n### D2C-030-027（P2，E2）：EDDY readout time 接受范围宽于 FSL\n\n当前 [`eddy.py:3240-3241`](../src/dwi2cond_xp/preprocessing/eddy.py#L3240) 只要求 readout\n有限且大于零。FSL `/usr/local/fsl/src/eddy/EddyHelperClasses.cpp:161-170` 拒绝\n`<0.01 s` 或 `>0.2 s`。\n\n因此 `0.009` 或 `0.201` 会被当前实现接受、被官方拒绝；端点 `0.01/0.2` 应接受。\n\n**影响**：这是正式输入验证合同差异；对合法区间内计算无影响。\n\n**验收要求**：官方兼容入口固定闭区间 `[0.01,0.2]`，覆盖两个端点及端点外值。"
      },
      {
        "id": "section_7",
        "type": "markdown",
        "body": "## 7. 第二轮遗漏扫描确认的问题\n\n### D2C-030-028（P2，E3）：affine vecreg 把非有限 tensor 静默清零\n\n[`registration.py:214-225`](../src/dwi2cond_xp/registration.py#L214) 把任何含 NaN/Inf 的\n采样 tensor 标成 invalid，随后统一写零。FSL `vecreg.cc:298-357` 直接插值、旋转并写出，\n非有限值会传播，而不是被伪装成合法零张量。\n\nidentity、`3×3×3` SPD tensor、中心 `Dxx=NaN` 的真实 A/B：FSL 中心六分量全 NaN，\n线性插值邻域合计 48 个 non-finite component；本地中心全零，输出 non-finite 数量为零。\n复现目录：`/tmp/d2c-vecreg-nan-audit.NN4PbJ/`。\n\n**影响与验收**：外部 prefit tensor 或坏 grad-dev 可被静默隐藏，后续 zero-tensor 修补又可能\n把它变成看似合理的各向同性 conductivity。strict 路径应在写出前拒绝；只有显式 robust\n模式可清零，并必须记录数量。正常有限自动 T1 主路径不受影响。\n\n### D2C-030-029（P1，E3）：公开 world-transform 路径在错误基底中重定向 tensor\n\n[`registration.py:186-198`](../src/dwi2cond_xp/registration.py#L186) 在没有显式\n`reorientation_transform` 时，直接从 input-world→reference-world 矩阵提取 polar rotation；\n[`cli.py:770-794`](../src/dwi2cond_xp/cli.py#L770) 的 `register-tensor --world-transform`\n只能走这个默认。FSL `vecreg.cc:265-273,342-357` 从 **FSL scaled-mm matrix** 提取旋转；\n世界矩阵在源/参考 NIfTI basis 不同的时候不能直接替代它。\n\n源 grid 旋转 90°、参考 grid identity、world transform identity 的真实 A/B：\n\n```text\nFSL center tensor          [2,0,0,3,0,1]\ncurrent default center     [3,0,0,2,0,1]\nrelative L2                0.377964473\nmax abs                    1.0\n```\n\n显式把正确 FSL matrix 作为 `reorientation_transform` 后与 FSL 全数组 exact。复现目录：\n`/tmp/d2c-world-basis-audit/`。自动 T1 路径已在\n[`t1_registration.py:409-420`](../src/dwi2cond_xp/preprocessing/t1_registration.py#L409)\n显式传 `primary.matrix`，故该主路径安全；公开 CLI/API 和其下游方向性 conductivity 受影响。\n\n**验收要求**：由 world transform、source grid 和 reference grid 计算 FSL scaled-mm matrix；\n覆盖 oblique、不同 handedness、identity/non-identity world transform 的真实 vecreg A/B。\n\n### D2C-030-030（P3，E3）：DTIFIT 派生 NIfTI metadata 未复现 FSL\n\n数值 dtype、scaling、qform/sform code 和矩阵一致，但正式 header 有三组差异：\n\n- [`nifti_fit.py:22-37,56-59`](../src/dwi2cond_xp/nifti_fit.py#L22) 把 V1/V2/V3 intent\n  写为 generic `vector`（1007），FSL `dtifit.cc:697-705` 写\n  `NIFTI_INTENT_RGB_VECTOR`（2003）；\n- FSL newimage 写 `xyzt_units=10`，本地继承输入；fixture 输入单位未知时本地为 0；\n- FSL 给 FA、MO、V、L/MD/tensor、S0/SSE 设置显示 calibration，本地保留 0/输入值。\n\n实测目录：`/tmp/d2c-dtifit-audit.FD8U3q/`。数组不变，但 RGB-vector intent 会影响方向\n彩色显示或依赖 intent 的消费者；calibration 主要影响显示。应作为一项 metadata 修复，\n不要拆成多个问题。\n\n### D2C-030-031（P1，E3）：`--no-repol` 错误关闭了参数估计期的 outlier 处理\n\n当前 [`eddy.py:2813-2828`](../src/dwi2cond_xp/preprocessing/eddy.py#L2813) 仅在\n`replace_outliers=True` 时检测并在 iteration>0 替换坏片，`:2934-2946` 也只在该条件下\n做 final check；`--no-repol` 因而把检测全过程关闭。\n\nFSL `eddy.cpp:544` 对 DWI 无条件调用 `Register(...,dol=true)`：即使没有 `--repol`，仍在\n各轮检测、临时替换坏片以稳健估计 movement/EC，最后才在写正式图像前用\n`RecycleOutliers()` 恢复原始坏片；final outlier report 也仍会写。`--repol` 控制的是正式\n输出是否保留 replacement，不是是否在参数估计中完全关闭 outlier logic。\n\n公开四坏片 fixture 的 no-repol A/B：FSL 仍准确报告 4 个坏片，本地 map 全零；corrected\nDWI relative L2 `0.113924`、movement `0.32143`、EC9 `0.72691`。复现目录：\n`/tmp/dwi2cond-v030-eddy-norepol.6dabpc/`。\n\n**影响与验收**：这是公开 `--no-repol` 控制路径，不影响 SimNIBS 默认 `--repol` 主调用；\n实现必须始终保留估计期 detection/temporary replacement，只有最终输出按开关 recycle。\n\n### D2C-030-032（P2，E3）：DTIFIT float mask coercion 与 FSL 不同，QA 计数又不一致\n\n当前 [`nifti_fit.py:122,409`](../src/dwi2cond_xp/nifti_fit.py#L122) 直接以 `mask>0`\n选择体素；FSL `dtifit.cc:435-440,552` 先把 mask 读入 `volume<int>`，再判断 `>0`，所以\n`0.5/0.999` 会先截成 0。另一方面，本地 `nifti_fit.py:260` 用 `count_nonzero` 统计\nmasked voxels，负值会进入 QA 计数，却不会参与实际拟合。\n\nmask `[0.5,-1]` 的真实 A/B：FSL 两体素 tensor 全零；本地首体素拟合出非零 tensor，\n负值体素又被错误计入 `masked_voxels`。复现目录：`/tmp/d2c-mask-audit.q4N9hc/`。\n自动 BET/CHARM 二值 mask 不受影响；public/custom mask 受影响。验收覆盖\n`0,0.5,0.999,1,1.5,-1`，并统一实际 fit、进度和 QA 数量。\n\n### D2C-030-033（P2，E3）：公开 vecreg mask 的负值语义与 FSL 不同\n\naffine [`registration.py:137-161,217-225`](../src/dwi2cond_xp/registration.py#L137) 和\nnonlinear [`nonlinear.py:455-484`](../src/dwi2cond_xp/preprocessing/nonlinear.py#L455)\n均用 `>0`；FSL `vecreg.cc:260-295,426-449` 仅在值 `==0` 时排除，负非零值属于 support。\n\nidentity 注册、全 `-1` mask 的 A/B：FSL support `210`，本地 support `0`。复现目录：\n`/tmp/d2c-vecreg-mask-audit.xpDVov/`。自动二值 mask 不受影响；compatibility API 应复现\nnonzero 语义，或显式强制/验证二值 mask。\n\n### D2C-030-034（P2，E3）：non-finite grad-dev 失败前已写出坏 tensor/valid mask\n\n[`nifti_fit.py:228-234`](../src/dwi2cond_xp/nifti_fit.py#L228) 只检查 grad-dev shape/affine，\n不检查 finite；主 tensor 与 valid mask 在 `:333-353` 先写出，`:354-361` 的 decomposition\n才因 NaN/Inf 抛错。\n\n首 voxel grad-dev 含 NaN 的真实对照：FSL return `-6`、`pinv(): svd failed`，无正式输出；\n本地虽然最终抛 `ValueError`，却遗留含 6 个 NaN 的 tensor 和把该 voxel 标 valid 的 mask。\n复现目录：`/tmp/d2c-graddev-nonfinite-audit2/`。\n\n**验收要求**：拟合前验证 grad-dev finite；所有派生产物写 attempt 临时路径，全部成功后\n原子提交；NaN/Inf、workers=1/>1 均应失败且零正式残留。该问题会放大 `D2C-030-022`，\n但 standalone `fit_dti_nifti` 也可达，因此不能只靠 pipeline 修复。\n\n### D2C-030-035（P2，E2）：simulation 输入验证未执行已公开的 finite/grid 合同\n\n[`simulation.py:49-99`](../src/dwi2cond_xp/simulation.py#L49) 只验证 tensor shape/affine，\n不读取并检查 tensor finite，也不确认 `T1` 与 `final_tissues` 的 shape/affine 一致。\n`docs/INPUT_CONTRACT.md` 却明确要求 tensor finite，并承诺 shape/affine/finite failure 显式报告。\n\n可执行反例中，一个 tensor voxel 含 NaN 时 `validate_simulation_inputs(mode=\"vn\")` 通过，\n`tensors_to_conductivity` 随后产生全 NaN；另一个 scalar dry-run 接受 shape/affine 都不同的\nT1 与 final_tissues。真实后端可能稍后失败，但不能把昂贵 solve 后的偶然失败当作输入验证。\n\n**验收要求**：建立共同 NIfTI contract validator；各向异性模式在 SESSION 创建前检查\ntensor finite，所有模式检查 T1/final_tissues grid；记录 checked voxel 数和 grid identity。\n\n### D2C-030-036（P2，E2）：register-tensor source mask 只查 shape，不查 affine\n\n[`registration.py:105-147`](../src/dwi2cond_xp/registration.py#L105) 对 source mask 只检查\nshape；同函数对 reference mask 却检查 shape+affine。相同 shape、affine 平移一 voxel 的\nsource mask 会被按 tensor 数组下标直接使用，实测可静默错选 valid support。\n\nFSL vecreg 同样假定调用者提供正确 grid，因此这是项目公开 API/文档安全合同缺口，不是\nFSL 数学公式差异。验收应在采样前要求 source mask 与 tensor grid exact，并覆盖\nsame-shape/different-affine 反例。\n\n### D2C-030-037（P2，E2）：`run_tdcs` 的构建/后处理异常会把 manifest 永久留在 running\n\n[`simulation.py:275-304`](../src/dwi2cond_xp/simulation.py#L275) 只把 `run_simnibs` 放进\ntry/except；`build_tdcs_session` 与完成后的 subject-volume masking 在 try 外。非法 solver\n的最小复现中函数抛 `ValueError`，但已经写出的 manifest 仍为 `\"running\"`；后处理 grid\n失败也有同样状态污染。\n\n**验收要求**：从 SESSION build 到所有 postprocess/manifest finalize 统一纳入事务；任意\n异常都原子写 `failed`、error type/message，并区分 solve 已完成但后处理失败的状态。\n\n### D2C-030-038（P1，E2）：TOPUP `iout` 恢复了错误的逐 volume 强度比例\n\n[`topup.py:2473-2479`](../src/dwi2cond_xp/preprocessing/topup.py#L2473) 把两个输入分别缩放\n到均值 100；[`topup.py:2593-2595`](../src/dwi2cond_xp/preprocessing/topup.py#L2593)\n输出时又分别乘回各自的 `input_means[index]/100`。\n\nFSL `topup.cpp:132-142` 虽也分别标准化输入，却另外计算所有输入原始均值的 `gmean`；\n`topup.cpp:227-236` 和 `topup_costfunctions.cpp:1104-1127` 对全部 corrected volumes 使用\n同一个 `gmean/100`。SimNIBS 固定配置启用 `--scale=1`，所以该分支真实可达。\n\n最小反例把 reverse-PE b0 的整体强度设为 forward 的两倍：场估计前两者都会归一至 100；\nFSL `iout` 两卷恢复相同公共尺度，本地却恢复约 `1:2`。这改变 corrected pair 的数值合同。\nSimNIBS 通常只取第一卷做 BET，纯全局尺度下 mask 大体不变，但固定绝对阈值或直接消费\ncorrected pair 会受影响。\n\n**验收要求**：全部 corrected volumes 统一使用 `mean(input_means)/100`；增加不等均值双 b0\n的真实 FSL A/B，同时保留等均值回归。\n\n### D2C-030-039（P1，E2）：legacy MCFLIRT 缺少官方薄层 `fix2D` 分支\n\n当前 [`legacy.py:76-150`](../src/dwi2cond_xp/preprocessing/legacy.py#L76) 始终运行普通三维\n多阶段注册，[`rigid.py:668-704`](../src/dwi2cond_xp/preprocessing/rigid.py#L668) 也始终开放\n三维参数。FSL `mcflirt.cc:367-397` 在 `zsize<3`、z-FOV `<20 mm` 或显式 2D 时复制端层、\n设置 `twodcorrect=1/smoothsize=0.1`；`mcflirt.cc:220-243` 只优化 `Rz/Tx/Ty`。\n\n对 `16×16×2` 或 z-FOV 小于 20 mm 的有效薄层输入，本地会开放 FSL 明确禁止的 out-of-plane\n自由度，矩阵与重采样可能不同。正常全脑三维 DWI 不受影响。\n\n**验收要求**：实现同一 `fix2D` 判断、端层复制和三参数优化；若固定子集决定不支持，应在\n入口显式拒绝，不能静默运行另一算法。\n\n### D2C-030-040（P2，E2）：EDDY 退化 outlier 统计的失败语义不同\n\n[`eddy.py:3626-3630,3653-3656`](../src/dwi2cond_xp/preprocessing/eddy.py#L3626) 在少于两个\neligible slices 或零方差时主动抛错；FSL `EddyHelperClasses.cpp:578-629` 继续做除法，后续\nNaN comparison 通常为 false，因此不标记 outlier 并继续。\n\n这不是正常全脑主路径问题，但 strict compatibility 对小 z、全相同 residual 或退化 mask\n会一边失败、一边完成。应选择复现 FSL，或在公开合同中把这些输入显式排除并在计算前给出\n稳定错误；测试需冻结两种退化条件。\n\n### D2C-030-041（P2，E2）：FNIRT 缺少官方 self-registration identity 快路径\n\nFSL `fnirt.cpp:130-140`、`fnirtfns.cpp:1133-1175` 在 input 与 reference 是同一图像且 affine\n为 identity 时直接写 identity deformation/Jacobian；当前\n[`fnirt.py:2333-2547`](../src/dwi2cond_xp/preprocessing/fnirt.py#L2333) 仍进入完整四级优化。\n\n正常 FA→T1 主路径不会触发，但 public API 对同一有效输入的控制流、耗时和官方 bitwise\nidentity 保证不同。本轮尚未以 E3 证明本地优化后的正式数组偏离 identity，因此本项保留\nP2，不能表述成已经确认的科学数组错误。应实现快路径，或明确排除 self-registration；\n增加 coefficient、field、Jacobian 和 warped image identity 测试。"
      },
      {
        "id": "section_8",
        "type": "markdown",
        "body": "## 8. 已确认一致或已有强正证据的部分\n\n这些结果限定于列出的输入、形状、精度和阶段，不能外推为完整 pipeline 等价。\n\n### 8.1 有限正常输入的 DTIFIT WLS\n\n六分量顺序 `Dxx,Dxy,Dxz,Dyy,Dyz,Dzz`、交叉项、信号平方权重、bvec 单位化和九分量\ngradient nonlinearity 均与 `dtifit.cc` 一致。全九分量非对角 grad、非单位 bvec fixture：\n\n```text\ntensor relative L2 = 6.47666e-8\ntensor max abs     = 1.16415e-10\nS0                 = exact\n```\n\n这支持有限正常输入、二值 mask 和 finite grad-dev 的局部实现，不覆盖\n`D2C-030-015` 及第二轮发现的 mask/非有限 grad-dev 边界。\n\n### 8.2 affine vecreg 与 PPD 数学\n\n- affine component mapping、polar rotation、`R D R^T`、source mask 与 T1 post-mask 一致；\n- 真实 affine fixture support 为 `215/215`，tensor relative L2 `8.75873e-8`，max abs\n  `1.74623e-10`；\n- nonlinear PPD 的前两个主特征方向映射与 FSL `vecreg.cc:170-190` 数学对应；真实 shear\n  focused test 为 `1 passed`。\n- 额外 oblique、不同 handedness、边界半体素、空间变化 tensor 和重复主特征值探针均与\n  FSL 对应；重复特征值 PPD relative L2 `1.45e-7`、max abs `1.91e-6`。\n\n这些正证据适用于显式提供正确 FSL reorientation matrix、finite tensor 和正确 grid mask\n的路径；不能外推到公开 world-transform 默认基底、非有限输入和错误 mask affine。\n\n### 8.3 conductivity `dir/vn/mc`\n\nFSL basis correction、特征值修复、体积加权、global intensity scale、zero-tensor 后修复\n均与 SimNIBS 4.6 `cond_utils.py` 对应。真实 `sphere3.msh` 上 `dir/vn/mc` 及多种\noblique/shear/zero-sampled 情形：\n\n```text\nrelative L2: 0 .. 4.18e-16\nmax abs:     <= 7.11e-15\n```\n\n这属于舍入级一致；但真实 `TDCSLEADFIELD` solve 尚未运行，不能外推到 FEM endpoint。\n\n### 8.4 legacy 当前公开 regression gate\n\n当前 tag 的冻结 comparator `16/16` 通过，关键指标为：\n\n| 指标 | 当前值 |\n| --- | ---: |\n| nodif relative L2 | `9.48847e-5` |\n| pass1/pass2 series relative L2 | `0.00059755 / 0.00102061` |\n| corrected DWI/mean relative L2 | `0.00544438 / 0.00773141` |\n| mask Dice | `0.9931947` |\n| final matrix max abs | `0.0442867 mm` |\n| tensor common-mask relative L2 | `0.7725999` |\n| FA relative L2 | `0.0300902` |\n| SSE relative L2 | `1.0676178` |\n| compat46 bvec | byte exact |\n\n必须注意：tensor 和 SSE 的门限分别宽到 `0.8` 和 `1.1`；fixture JSON 自己也明确称其为\nregression gate，而不是 equivalence。阈值和整改实现位于同一 commit，不能把“门禁通过”\n当作独立证明。它能防止大幅回退，但不能证明 whole-grid optimizer 数值相同。"
      },
      {
        "id": "section_9",
        "type": "markdown",
        "body": "## 9. 主动差异与需要冻结的兼容决策\n\n下列项目确实与官方字面行为不同，但当前证据不足以直接称为科学 bug；修改前应先明确\n项目要复现安装版字面行为，还是保留主动修正/统一 schema。\n\n### 9.1 GRE dwell time 精度\n\n当前使用 `milliseconds / 1000.0`；官方 shell 用 `bc scale=5` 截到小数点后五位。例如：\n\n```text\ninput     0.789 ms\ncurrent   0.000789 s\nofficial  0.00078 s\n```\n\n字面位移比例相差约 `1.15%`。若保留更精确值，应在文档中声明为主动修正并提供明确的\nliteral compatibility 策略；不能同时称“逐字复现 FSL shell”。\n\n### 9.2 已知默认策略与范围差异\n\n- 当前默认固定 `random_seed=1`；官方 SimNIBS 不传 `--initrand`，FSL 默认按当前时间设种。\n  严格 A/B 必须给 FSL 显式传 `--initrand=1`；\n- 当前 EDDY 主动限制单壳；FSL EDDY 本身可处理更广范围；\n- GRE 只接受 rad/s fieldmap，不覆盖 Siemens phase+TE→PRELUDE；\n- TOPUP 固定子集只接受 x/x-/y/y-；\n- 官方 SimNIBS 的负 PE 语法是 `-x/-y/-z`，项目公开语法是 `x-/y-/z-`；\n- full raw-GRE workflow 没有官方 `--dontdenoise` 等价开关，固定走 median filter；\n- 项目不生成 `first_ev_for_check.msh`，也不复刻 FSLeyes/Gmsh 交互检查和 HTML log；\n- FEM 未启用时项目不要求 `final_tissues.nii.gz` sentinel，官方 T1 path 会检查；\n- 项目保留 intermediates 和 artifact cache，官方 shell 默认清理若干中间目录；\n- legacy `compat46` 不旋转 bvec，与旧 SimNIBS `EDDY_CORRECT` 脚本一致；\n- GRE 应用 warp 的实现是对上游 `$# -gt 5` off-by-one 的 intended correction，不是安装版\n  shell 的 literal behavior；\n- external susceptibility field + no-TOPUP EDDY 是项目扩展；官方不允许 GRE fieldmap 与\n  EDDY 组合；\n- FEM 是本项目在官方 `dwi2cond` shell 之后增加的 consumer，不应混作 shell 等价性。"
      },
      {
        "id": "section_10",
        "type": "markdown",
        "body": "## 10. 已排除的误判\n\n### 10.1 无 field 时未运行 offset/movement separation\n\n这不是问题。当前仅在 prepared field 存在时运行 separation；FSL 无 field 时虽进入同名\n调用，但 `HasFieldOffset()==false`，立即 no-op。两侧计算等价，不应为了“命令顺序相同”\n增加无效逻辑。\n\n### 10.2 TOPUP movement 未直接传入 EDDY\n\n官方把 TOPUP basename（coefficient + movement）交给 EDDY，当前主要传 dense field。\n但 SimNIBS 固定 DWI index 全为 1，TOPUP 第一 scan 又是零运动参考，因此目前不能仅凭\n接口形状判定主路径有 movement bug。仍需 coefficient reader 与 dense field 的同输入\n插值 A/B；在有反例前应列为证据空白，不应写成确认缺陷。\n\n### 10.3 无 field 时 `.eddy_parameters` 被误读为 15 列\n\n这也是误判。`QuadraticScanECModel(field=false)` 仍分配 10 个 EC 参数；`_nepd=9` 只控制\n导数数量，`NParam()` 仍返回 10。`WriteParameterFile(ANY)` 按 `NParam()` 写出\n`6+10=16` 列，最后一列为零。源码位置为\n`/usr/local/fsl/src/eddy/ECModels.h:300,359,475-488` 和\n`ECScanClasses.cpp:1157-1168`。\n\n当前公开 fixture 实测 FSL 与本地 parameter table 都是 `(26,16)`。因此 15/16 列差异\n不得进入问题清单，也不需要为这一误判修改实现。\n\n### 10.4 特征向量逐体素正负号不同\n\n随机 504 个 SPD tensor 中，V1/V2/V3 分别有 `220/231/210` 个体素与 FSL 点积小于零；\n逐体素按轴符号对齐后最大绝对误差为零，L1/L2/L3 也 exact。DTI 特征方向是无向轴，\n`v` 与 `-v` 表示同一张量方向，PPD 与 conductivity 对该符号不敏感。这是允许的本征向量\n符号自由度，不是计算错误；验收应使用 axial angle，不应强迫逐元素同号。\n\n### 10.5 其他已核销候选\n\n- GRE 4-D magnitude：官方取第 0 volume，本地也取第 0 volume，不是差异；\n- legacy GRE affine+warp：本地行为是对官方 shell 参数计数 off-by-one 的 intended fix，\n  不是新的组合公式错误；\n- TOPUP LM/SCG/PCG、EDDY 普通 unit-damped GN accept/reject、FNIRT 四级 schedule：二轮\n  逐源码和 debug 未找到新的公式/控制流分歧；终点不 exact 已按证据边界报告，不能反向\n  虚构 bug；\n- conductivity 中看似反常的 FSL basis correction 与 normalization 次序：本地和 SimNIBS\n  `cond_utils.py` 相同，不应擅自“纠正”。"
      },
      {
        "id": "section_11",
        "type": "markdown",
        "body": "## 11. 当前验证记录及其边界\n\n### 11.1 本轮实际运行\n\n```text\n完整 pytest：575 passed, 9 skipped in 182.12 s\n显式 FSL focused batch：92 passed in 9.99 s，0 skip\ntensor/conductivity focused：93 passed in 1.32 s\n真实 PPD focused：1 passed in 0.56 s\nlegacy FSL comparator：16/16 gates passed\nstrict-FSL partial-NaN：确认失败，见 D2C-030-015\nEDDY default/no-repol/no-PEAS：三组当前 FSL endpoint A/B 完成\naffine basis/NaN、mask、grad-dev、pipeline/FEM 状态机：最小反例完成\n```\n\n完整 pytest 的 9 个 skip 是未在该命令中逐项注入可选 FSL executable；随后显式 FSL\n批次已实际执行相关 reference tests。测试总通过数说明当前回归没有普遍崩溃，但 coverage\n或测试通过数本身不证明未覆盖分支等价。\n\n本报告引用的 `/tmp/...` 目录是本轮工作站上的暂存探针证据，不是 release asset，也不是\n长期 provenance。仓库中已有的冻结 fixture/JSON 和测试可长期复核；其余 E3 反例在进入\n代码整改前，必须先把最小输入、命令、关键输出和 SHA-256 归档到持久测试/证据目录，并先\n冻结“修复前失败”。因此这些暂存目录足以支持本轮判定，但不能单独作为问题关闭证据。\n\n### 11.2 当前 commit 的公开 EDDY 同输入 A/B\n\n公开 `26×26×18×26` fixture、相同 seed `1`、8 workers、FSL 6.0.4 的当前重跑结果为：\n\n| 边界 | current 与 FSL 差异 |\n| --- | ---: |\n| corrected DWI（input mask 内） | relative L2 `0.00818859`；max abs `36.9247` |\n| outlier-free scan-space data（mask 内） | relative L2 `0.00232695`；max abs `49.6337` |\n| movement translation（列 1--3，mm） | relative L2 `0.0745655`；max abs `0.0749844 mm` |\n| movement rotation（列 4--6，rad） | relative L2 `0.0697547`；max abs `0.000773071 rad` |\n| DWI EC9（列 7--15；线性 Hz/mm、二次 Hz/mm²） | relative L2 `0.0179157`；max abs `0.00894537`（原生系数单位混合） |\n| rotated bvecs | relative L2 `0.000654262`；max abs `0.000756744` |\n| outlier map | 四个注入坏片 `(5,8)/(11,9)/(18,7)/(23,10)` exact |\n| parameter shape | 本地与 FSL 均为 `(26,16)`；无 field 时列 16 的 offset 均为零 |\n\n这组 E3 结果直接证明当前 endpoint 不是逐值等价；强坏片的 map 恰好相同也不能关闭\n`D2C-030-017`，因为 replacement、PEAS、parameters 和最终图像仍不同。复现目录：\n`/tmp/dwi2cond-v030-eddy-ab.H7VgMN/`。\n\n进一步把输入换成未污染 DWI，同时双方关闭 repol 与 PEAS，仍得到 corrected DWI\nrelative L2 `0.00242117`、movement `0.0259526`、EC9 `0.00491818`、bvec\n`0.0000860`。初始 GP 超参数和 prediction 基本一致，iter-0 update relative L2 约\n`0.00119`，到 iter-4 放大至约 `0.2219`。目前没有足够证据把这段剩余误差归因于新的\n控制流或公式 bug，故列为**未归因的 optimizer trajectory 数值缺口**，不额外编号。\n复现目录：`/tmp/dwi2cond-v030-eddy-norepol.6dabpc/`。\n\n### 11.3 当前 commit 的 FNIRT 同输入 A/B\n\n同一公开 fixture、完整 `subsamp=8,4,2,2` 四级配置的当前结果：\n\n| 边界 | current 与 FSL 差异 |\n| --- | ---: |\n| coefficients | relative L2 `0.118921`；max abs `0.575556`；correlation `0.99059` |\n| dense field | relative L2 `0.0857440`；max `0.431141 mm`；correlation `0.99489` |\n| Jacobian | relative L2 `0.0185281`；max `0.0809296` |\n| warped FA | relative L2 `0.00232771`；max `0.253809` |\n\n初始 cost exact 为 `1599.9456759944742`；gradient blocks relative error\n`8e-8..9e-7`、Hessian `1.21e-8`、首步 coefficient `8.74e-6`、单步 displacement\n`1.47e-5`。四级 schedule、mask、强度映射冻结、Jacobian/topology 控制流均未找到新的\n公式级分歧。当前证据支持“小的 solver/浮点差异经多级非线性迭代放大”，不能据终点误差\n虚构一个具体 bug；但同样不能称最终 coefficient/field 数值等价。\n\n### 11.4 仍未闭合的高价值证据\n\n- 修复 `016/017` 后，同一 interspersed-b0、near-threshold outlier fixture 的完整 EDDY\n  history/endpoint FSL A/B；\n- 当前 commit 上 nonzero reverse-PE 的完整 TOPUP→EDDY coefficient/movement endpoint；\n- 当前 commit 上真实 HCP legacy+FNIRT→PPD→post-mask 阶段级重跑；\n- FNIRT 每级 cost、accepted/rejected step、coefficient/field/Jacobian 的不可跳过轨迹门禁；\n- folded/near-singular active support 的完整 PPD failure/output FSL A/B；\n- 同一真实 m2m/cap/tensor 上 `scalar/vn/dir/mc` 的 SimNIBS FEM solve；\n- legacy comparator 的 tensor/SSE 窄容差、whole-grid 和病态 denominator 解释。\n\n### 11.5 报告可用性判定\n\n本报告在交付前经过三路只读复核：问题 ID/严重度机械重算、P0/P1 双侧源码抽查，以及\n版本、相对链接、关键数值和误判边界复核。复核后修正了 `038/039` 的严重度和 `019` 的\nEDDY mask 适用范围；没有 P0/P1 被驳回。当前判定为：**可用于制定整改计划，但需携带\n证据归档和未闭环 endpoint 两项 caveat**。它不是问题关闭报告，也不是正式等价证明。"
      },
      {
        "id": "section_12",
        "type": "markdown",
        "body": "## 12. 建议整改顺序与关闭标准\n\n### 第一批：阻断正式等价声明\n\n1. 修正 `D2C-030-017`：PEAS 后执行 final GP/repol，final fudge factor 固定为 1；\n2. 修正 `D2C-030-016`：实现 FSL interspersed-b0 判定和 movement interpolation；\n3. 修正 `D2C-030-029`：公开 world transform 转成正确 FSL scaled-mm reorientation matrix；\n4. 修正 `D2C-030-015`：strict-FSL 使用 FSL NaN/clamp 语义，robust 模式另行保留；\n5. 修正 `D2C-030-018`：按 FSL shell group/mean 判定单壳，并统一两个本地 validator；\n6. 修正 `D2C-030-031`：no-repol 保留估计期 outlier machinery，只在正式输出前 recycle；\n7. 修正 `D2C-030-038`：TOPUP `iout` 使用公共 gmean scale；\n8. 修正或显式排除 `D2C-030-039`：复现 MCFLIRT `fix2D`，或在入口拒绝薄层输入。\n\n退出条件不是“新增单元测试通过”，而是同一输入、相同 seed/线程/精度下，比较中间状态、\n参数 history、outlier map、rotated bvec、corrected DWI、tensor/S0/SSE，并说明每个容差来源。\n\n### 第二批：恢复正式数据谱系和可复现产物\n\n1. 拆分 raw/corrected DWI 与 mask，所有 QA 消费 producer artifact 并检查 affine/grid；\n2. official FNIRT wrapper 强制 T1 post-mask；\n3. FEM manifest/cache 纳入所有动态 output 的内容哈希；\n4. stage/DTIFIT 使用临时 attempt 目录和原子发布，关闭 `022/034` 的部分输出残留；\n5. affine strict 路径拒绝 non-finite tensor；simulation 在 solve 前落实 finite/T1-grid 合同；\n6. source mask 核对 affine；`run_tdcs` 将 build/solve/postprocess 全部纳入 failed-state 事务。\n\n### 第三批：输入、QA 和元数据\n\n1. 官方 QA b0 阈值固定为 0；\n2. 数值环境进入 cache identity；\n3. 未消费 config 按 mode 拒绝；\n4. 修正 tetra count；\n5. readout time 固定为 FSL 的 `[0.01,0.2]`；\n6. 统一 DTIFIT/vecreg mask coercion，并复现 FSL NIfTI intent/units/calibration；\n7. 实现或显式拒绝退化 EDDY outlier 和 FNIRT self-registration 边界；\n8. 冻结 GRE dwell precision；`.eddy_parameters` 15/16 已证伪，不需要修改。\n\n每个问题关闭时应单独记录：修复 commit、失败前反例、修复后同输入结果、误差、测试命令、\n未验证边界。不要再次只用总测试数或 coverage 百分比关闭算法问题。"
      },
      {
        "id": "section_13",
        "type": "markdown",
        "body": "## 13. 最终审计结论\n\n当前 v0.3.0 不是从零开始的错误实现：默认 legacy/T1 主干、有限正常输入 WLS、张量分量、\n给定正确 FSL matrix 的 affine vecreg、nonlinear PPD 和 conductivity 已有扎实的源码与\n数值正证据，旧审计的 14 项整改也确实改善了实现。\n\n但“多数阶段存在且测试通过”不等于“与官方相同逻辑”。本轮在当前 tag 上确认：\n\n- 默认 EDDY 有两个核心控制流差异，公开 no-repol 又有一个独立语义错误；\n- single-shell 输入判定、TOPUP `iout` 强度恢复和薄层 MCFLIRT 分支不同；\n- strict-FSL 对部分 NaN 有真实数值反例；\n- 公开 world-transform 可在不同 NIfTI basis 下旋转错 tensor；\n- raw QA、mask/grad-dev、FNIRT public API、FEM/cache 和 stage publication 仍有合同缺陷；\n- EDDY/FNIRT 当前 endpoint 仍有未归因数值差异，完整 TOPUP→EDDY、真实 HCP nonlinear\n  和真实 FEM endpoint 尚未闭环。\n\n因此应拆开两条关闭边界：\n\n1. 对“官方流程/数值等价”声明，至少先关闭 `015..020,029,031,038,039`，并补齐相应真实\n   同输入阶段级 A/B；\n2. 对“可靠端到端产品输出”声明，还必须关闭 pipeline/cache/FEM 的 `021,022` 以及会污染\n   正式产物的相关数据合同问题。\n\n在上述边界满足前，建议冻结任何“v0.3.0 与官方实现相同”“完整 FSL 等价”或“端到端\n数值等价”的对外表述；P2/P3 则必须按公开支持范围逐项关闭或明确排除。准确的当前表述\n应是：\n\n> dwi2cond-xp v0.3.0 实现了 SimNIBS 4.6 所需固定 FSL 子集的主要流程，并在若干局部\n> 算子和合成阶段取得数值对应；独立复审仍发现 EDDY/TOPUP 控制流与输出、strict-FSL\n> 边界语义、公开张量重定向和数据/cache/FEM 合同问题，完整同流程与端到端数值等价\n> 尚未证明。"
      }
    ]
  },
  "snapshot": {
    "version": 1,
    "generatedAt": "2026-08-26T00:00:00+08:00",
    "status": "ready",
    "datasets": {
      "severity_counts": [
        {
          "severity": "P0",
          "issue_count": 2,
          "scope": "标准有效输入核心控制流"
        },
        {
          "severity": "P1",
          "issue_count": 10,
          "scope": "有效分支、正式结果与科学QA合同"
        },
        {
          "severity": "P2",
          "issue_count": 13,
          "scope": "边界、复现与低风险辅助产物"
        },
        {
          "severity": "P3",
          "issue_count": 2,
          "scope": "元数据与诊断"
        }
      ]
    }
  },
  "sources": [
    {
      "id": "issue_inventory",
      "label": "dwi2cond-xp v0.3.0 独立复审问题清单",
      "path": "V0.3.0_POST_REMEDIATION_REAUDIT_REPORT_2026-08-26.md",
      "query": {
        "engine": "duckdb",
        "language": "sql",
        "sql": "SELECT * FROM (VALUES ('P0', 2, '标准有效输入核心控制流'), ('P1', 10, '有效分支、正式结果与科学QA合同'), ('P2', 13, '边界、复现与低风险辅助产物'), ('P3', 2, '元数据与诊断')) AS issue_inventory(severity, issue_count, scope)",
        "description": "按复审报告中27个唯一问题ID汇总严重度。",
        "executed_at": "2026-08-26T00:00:00+08:00"
      }
    }
  ]
}
